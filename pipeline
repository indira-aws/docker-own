pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/indira-aws/docker-own.git'
            }
        }

        stage('build') {
            steps {
                sh 'docker build -t $image .'
            }
        }

        stage('tag') {
            steps {
                sh 'docker tag $image $repo'
            }
        }

        stage('push') {
            steps {
                sh 'docker login -u indunp -p $password'
                sh 'docker push $repo'
            }
        }

        stage('deploy') {
            steps {
                sh 'kubectl apply -f deployment.yml'
                sh 'kubectl apply -f service.yml'
            }
        }
    }
}
